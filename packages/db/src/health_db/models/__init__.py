"""SQLAlchemy models.

TODO (M1): Define the tables from PRD section 7 (household, user, api_key, goal,
device, metric_sample, daily_summary, meal, lab_report, biomarker, conversation,
message, briefing, audit_log). Empty in M0; `/db-ping` only needs `SELECT 1`.
"""
