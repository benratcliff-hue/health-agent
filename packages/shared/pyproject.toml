[project]
name = "health-shared"
version = "0.0.0"
description = "Shared types, constants, and prompt templates for Personal Health Agent"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/health_shared"]
